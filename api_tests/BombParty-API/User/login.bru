meta {
  name: login
  type: http
  seq: 2
}

post {
  url: {{urlApi}}/user/login
  body: json
  auth: none
}

body:json {
  {
    "email" : "bla@mail.com",
    "password" : "azerty"
  }
}

vars:pre-request {
  urlApi: http://localhost:7774/api/v1/
}

script:post-response {
  var jwt = res.body.token;
  bru.setEnvVar("jwtToken",jwt)
}

settings {
  encodeUrl: true
  timeout: 0
}
